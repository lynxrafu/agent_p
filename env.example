# =============================================================================
# Agent_P Environment Configuration
# =============================================================================
# INSTRUCTIONS:
# 1. Copy this file to `.env` in the same directory
# 2. Replace the placeholder values with your actual API keys
# 3. Restart the application (docker-compose down && docker-compose up -d)
# =============================================================================

# --- Infrastructure (no changes needed for Docker) ---
MONGODB_URL=mongodb://mongo:27017
REDIS_URL=redis://redis:6379
LOG_LEVEL=INFO

# --- API Keys (REQUIRED - get free keys from links below) ---

# Google Gemini API Key (FREE)
# Get yours at: https://aistudio.google.com/apikey
GOOGLE_API_KEY=your-gemini-api-key-here

# Tavily Search API Key (FREE - 1000 searches/month)
# Get yours at: https://tavily.com/
TAVILY_API_KEY=your-tavily-api-key-here

# --- Model Selection ---
GEMINI_MODEL=gemini-3-flash-preview

# --- Optional: API Security (leave empty to disable) ---
# API_KEY=your-secret-api-key
# API_KEY_HEADER=X-API-Key

# --- Optional: Rate Limiting (disabled by default) ---
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_REQUESTS_PER_MIN=60
# RATE_LIMIT_BURST=10

# --- Optional: Session Management ---
# SESSION_TTL_MINUTES=30
